spring:
  application:
    name: service-product #服务名称
  cloud:
    config:
      uri: http://localhost:9001 #配置服务的地址 bootstrap.yml
      enabled: true #开启配置
      profile: dev #版本
      label: "" #git配置的分支信息，master类似的
